# Manual Integration (legacy)

Manual framework embedding applied to the legacy Falcon iOS SDK, which was
distributed as a static `.xcframework` file.

The current SDK is distributed exclusively via **Swift Package Manager**. See
the [iOS Integration guide](/integration-guide/ios/integration) for installation
and usage instructions.
